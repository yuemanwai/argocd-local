apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fyp-app.fullname" . }}-test-connection"
  labels:
    {{- include "fyp-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fyp-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
