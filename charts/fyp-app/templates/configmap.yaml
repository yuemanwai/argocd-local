apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fyp-app.fullname" . }}
  labels:
    {{- include "fyp-app.labels" . | nindent 4 }}
data:
  {{- with .Values.configMap }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
