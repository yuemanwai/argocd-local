apiVersion: v1
kind: Service
metadata:
  name: {{ include "fyp-app.fullname" . }}
  labels:
    {{- include "fyp-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fyp-app.selectorLabels" . | nindent 4 }}
