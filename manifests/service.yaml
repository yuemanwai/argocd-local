apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-nginx
  name: my-nginx-svc
spec:
  ports:
  - name: 8080-80
    port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: my-nginx
  type: NodePort

