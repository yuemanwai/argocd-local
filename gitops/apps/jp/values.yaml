# Default values for jp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# ================================================================
# Deployment Settings
# ================================================================
# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1
# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: docker.io/yuemanwai/fyp-jp
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: 0b621a6
# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""
# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""
# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}
podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
# capabilities:
#   drop:
#   - ALL
# readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  requests:
    cpu: "0.25"
    memory: 512Mi
  limits:
    cpu: "0.5"
    memory: 1024Mi
deploymentAnnotations:
  argocd.argoproj.io/ignore-differences: |
    - group: apps
      kind: Deployment
      jsonPointers:
      - /spec/replicas
# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
startupProbe:
  httpGet:
    path: /startup
    port: http
  failureThreshold: 30
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /readyz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 20
  timeoutSeconds: 2
  failureThreshold: 3
# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}
tolerations: []
affinity: {}
# ================================================================
# Service Settings
# ================================================================
# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 5000
# ================================================================
# Horizontal Pod Autoscaler Settings
# ================================================================
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 60
  targetMemoryUtilizationPercentage: 60
# ================================================================
# Job Settings - init-db and seed test-data
# ================================================================
job:
  backoffLimitCount: 4
# ================================================================
# Pod Disruption Budget
# ================================================================
podDisruptionBudget:
  minAvailable: 1
# ================================================================
# Database Settings
# ================================================================
database:
  # PostgreSQL image configuration
  image:
    repository: postgres
    tag: "16-bookworm"
    pullPolicy: IfNotPresent
  # Database name
  name: "postgres"
  # Database credentials
  username: "postgres"
  password: "postgres"
  # Database port
  port: 5432
  # Resource limits
  resources:
    requests:
      cpu: "0.25"
      memory: 256Mi
    limits:
      cpu: "0.5"
      memory: 512Mi
  # Persistent volume configuration
  persistence:
    enabled: true
    accessMode: ReadWriteOnce
    size: 1Gi
# ================================================================
# ConfigMap Settings
# ================================================================
# Note: DB_HOST, DB_PORT, DB_NAME are auto-injected from database config
configMap:
  SQLALCHEMY_TRACK_MODIFICATIONS: "false"
  SESSION_SQLALCHEMY_TABLE: "flask_sessions_data"
  # Mail server configuration
  MAIL_SERVER: "mailhog"
  MAIL_PORT: "1025"
  MAIL_USE_TLS: "true"
  # Logging configuration
  LOG_TO_STDOUT: "true"
  LOG_LEVEL: "INFO"
  # other configurations
  ADMINS: "admin@example.com"
  POSTS_PER_PAGE: "10"
  LANGUAGES: "en, es, zh"
# ================================================================
# Secret Settings
# ================================================================
# Note: DB_USERNAME, DB_PASSWORD are auto-injected from database config
secret:
  # Flask secret key
  SECRET_KEY: "just-a-placeholder"
  # Mail server credentials (if MailHog doesn't require a password, you can omit this)
  # MAIL_USERNAME: <mail_user>
  # MAIL_PASSWORD: <mail_pass>
  # API Keys
  # GEMINI_API_KEY: <Gemini API Key>
# ================================================================
# Service Monitor Settings
# ================================================================
serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack
  path: "/metrics"
  interval: "30s"
# ================================================================
# Dashboard ConfigMap Settings
# ================================================================
dashboard:
  enabled: true
  datasourceUid: prometheus # Grafana Prometheus datasource UID
# ================================================================
# Ingress Settings
# ================================================================
# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
