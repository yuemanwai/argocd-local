apiVersion: v1
kind: Service
metadata:
  labels:
    app: fyp-app
  name: fyp-app-svc
spec:
  ports:
  - name: 8080-5000
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: fyp-app
  type: NodePort

